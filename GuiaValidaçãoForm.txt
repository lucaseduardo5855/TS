Validação de formulario com TS

. Html e Css prontos 

. pegar as classes deles

. capturar o evento de submit do form e tipando o elemento dentro

. no form vamos tirar o envio do form e validar se está ok via console

. função se existir alguma msg de error sendo exibida quero ocultar ela no input (hide) 

.como estamos usando o addventilester temos o resto como um corpo dentro do Hide

. vamos fazer função de error de mensagem onde pegamos o input e msg e retornamos nada nela (ShowErrorM), vamos pegar a classe do formF com input e tipar ela dentro da constante (lembrando que temos que pegar o pai do input, apenas do input), e tb a de errorM vamos pegar a classe dele 

.vamos passar o ErrorMessage para mensagem

.e o formF vai ter uma lista que adiciona a classe de erro q setamos no inicio e tem no css, pq ela é um class do css quando é erro

.vamos testar o showError no input de usuario com uma msg.

.vamos chamar o hide, mas na função vamos ocultar ela, entao vamos pegar todos os forms ali um '.' e a classe do css e fazer um for nelas removendo a classe mesmo delas

resumo: for selecionando todas as msg e removendo os elemento q tem elas

obs: n precisamos mais do show e do hide no final ali q usamos p testar as mensagem com input pq ja temos os 2 agora.

.Agora vamos fazer as validações

.a primeira vamos checkar os campos vazis e passar os campos q queremos checkar

.vamos fazer a função dela, onde pegamos todos os input e fazemos o for, onde teremos a msg q n pode ficar vazio os campos.

obs: vamos validar se deu msg de erro em todos os inputs ali

.Checkar o email, e vamos importar o isEmail do validator

e vamos fazer a função e validar e retornando msg

.Checkar agora as 2 senhas se está de acordo

.fazer nada -> tudo ok agora vamos clicar no form e ver se ele envia um console log
p isso vamos fazer o forms no forms fields com a classe de erro q tem algum campo com erro e n envia.

.entao vamos criar a função temos q enviar formulario em ingles, e nela vai ter retorno de sim ou nao, lembrando q temos o css com a classe de erro.
vamos fazer uma variavel q pode ser mudada q começa verdadeira 
e vamos pegar todos so formulario e passar para todos o erro da constante, e se tiver erro vai cair dentro de um forEach e vai retornar falso

.e la em cima vamos fazer o se o formularioforenviado e tiver formulario dentro dele q é o outro corpo la, vai retornar console.log mas podedmos enviar com ajax tb. ou submit.

.

.

.

.

.

.

.

.

.

.


